import React, { Component } from "react";
import styled from "styled-components";
import { Page, SubTitle } from "../shared/styledComponents";

export const Location = () => {
  return <SubTitle>Location</SubTitle>;
};
